package protocol

// Constantes de versão do protocolo
const (
	// CurrentVersion é a versão atual do protocolo
	CurrentVersion uint8 = 1
)

// Tipos de pacotes
const (
	// PacketTypeMessage representa um pacote de mensagem
	PacketTypeMessage MessageType = 0x04
)
